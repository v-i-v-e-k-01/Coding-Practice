#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,k,count;
    cin>>n>>k;
    vector<int> ar;
    for(int i=0;i<n;i++)
    {
        cin>>ar[i];
        int len=ceil(log10(ar[i])),temp;
        for(int j=0;j<len;j++)
        {
            temp=
        }

    }
}